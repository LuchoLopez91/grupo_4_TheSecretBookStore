<!DOCTYPE html>
<html lang="es">
<%- include("../partials/head") %>  

  <body class="register-login">
    
    <%- include("../partials/header") %> 

    <div class="form-container">
      <h1>Bienvenidos a </h1>
      <h1 class="web_name">The Secret Bookstore</h1>
      <h2>Crea tu cuenta</h2>
        <form action="/users/register" method="post" enctype="multipart/form-data">
        <div class="form">
          <div class="input-container">
            <label for="name">Nombre</label><br />
            <input
              class="input-style"
              type="text"
              name="name"
              id="name"
              placeholder="" 
              value="<%= locals.old && old.name %>"
            />
            <% if (locals.errors && errors.name) { %>
              <small class="text-danger"><%= errors.name.msg %> </small>
          <% } %>
          </div>
          <div class="input-container">
            <label for="last_name">Apellido</label><br />
            <input
              class="input-style"
              type="text"
              name="last_name"
              id="last_name"
              placeholder=""value="<%= locals.old && old.last_name %>"
            />
            <% if (locals.errors && errors.last_name) { %>
              <small class="text-danger"><%= errors.last_name.msg %> </small>
          <% } %>
          </div>

          <div class="input-container">
            <label for="email">E-mail</label><br />
            <input
              class="input-style"
              type="email"
              name="email"
              id="email"
              placeholder=""
            />
            <% if (locals.errors && errors.email) { %>
              <small class="text-danger"><%= errors.email.msg %> </small>
          <% } %>
          </div>
          <div class="input-container">
            <label for="pass1">Contraseña</label><br />
            <input class="input-style" type="password" name="pass1" id="pass1" />
            <% if (locals.errors && errors.pass1) { %>
              <small class="text-danger"><%= errors.pass1.msg %> </small>
          <% } %>
          </div>
          <div class="input-container">
            <label for="pass2">Repetir contraseña</label><br />
            <input class="input-style" type="password" name="pass2" id="pass2" />
            <% if (locals.errors && errors.pass2) { %>
              <small class="text-danger"><%= errors.pass2.msg %> </small>
          <% } %>
          </div>
          <div class="input-file">
            <label for="avatar"></label>
            <input type="file" name="avatar" id="avatar" accept=".jpg, .png" class="avatar_img" />
          </div>
          <button type="submit">CREAR NUEVO USUARIO</button>
        </div>
      </form>
      <p>¿Ya tienes cuenta? <a href="/login">Iniciar sesión</a></p>
    </div>

    <%- include("../partials/footer2") %> 
    <%- include("../partials/scripts")  %>  

  </body>
</html>
